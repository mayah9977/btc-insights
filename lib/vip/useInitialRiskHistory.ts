'use client'

/**
 * ⛔ Completely Disabled: useInitialRiskHistory
 *
 * - 서버 fetch ❌
 * - hydrate ❌
 * - backfill ❌
 * - history store 접근 ❌
 *
 * 현재 앱 구조에서 Risk 초기 히스토리 기능을 완전 제거합니다.
 */

export function useInitialRiskHistory(): void {
  // 🔥 완전 비활성화
  return
}
