"use strict";

/**
 * ⚠ DISABLED FILE
 *
 * OI is now handled via Binance Futures WebSocket (markPrice@1s).
 * This REST poller is deprecated.
 *
 * Do NOT re-enable unless architecture changes.
 */

console.warn(
  "[OI POLLER] DISABLED - handled by WebSocket stream",
);

// 실행 즉시 종료 (안전)
process.exit(0);
